# Yourtebu

Ad-free YouTube PWA with background play, audio-only mode, sleep timer, and livestream viewing.

## Setup

```bash
pnpm install
pnpm dev
```

## Structure

- `packages/shared` - i18n, Zod schemas, utilities
- `packages/server` - Hono API proxy
- `packages/web` - Vite PWA frontend
